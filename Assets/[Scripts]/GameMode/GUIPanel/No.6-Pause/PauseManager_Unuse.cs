using UnityEngine;
using UnityEngine.UI;
using DG.Tweening;

public class PauseManager_Unuse : MonoBehaviour
{
    /*
    public RectTransform gradientPanel;
    public RectTransform retryButton;

    public GameObject pauseButton;
    public GameObject resumeButton;

    [Header("イージングの種類")] public Ease easeType;

    public void Pause()
    {
        gradientPanel.DOAnchorPosX(0, 0.15f).SetEase(easeType);
        retryButton.DOAnchorPosX(0, 0.15f).SetEase(easeType);
        pauseButton.SetActive(false);
        resumeButton.SetActive(true);
        DOTween.To(() => Time.timeScale,
            x => Time.timeScale = x,
            0f, 0.2f)
            .SetEase(easeType);

    }

    public void Resume()
    {
        gradientPanel.DOAnchorPosX(540, 0.15f).SetEase(easeType);
        retryButton.DOAnchorPosX(150, 0.15f).SetEase(easeType);
        pauseButton.SetActive(true);
        resumeButton.SetActive(false);
        DOTween.To(() => Time.timeScale,
            x => Time.timeScale = x,
            1f, 0.15f)
            .SetEase(easeType);
    }
     */
}
